<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Y 16 Fries | Premium Menu</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #FF5E13; /* Orange color [cite: 3] */
            --secondary: #2D2D2D;
            --accent: #FFD700;
            --white: #ffffff;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Montserrat', sans-serif; } [cite: 4]

        body { 
            background: #1a1a1a; 
            display: flex; 
            justify-content: center; 
            align-items: center; 
            min-height: 100vh;
            color: var(--secondary);
        } [cite: 5, 6]

        .phone-frame {
            width: 380px;
            height: 780px;
            background: #fdfdfd;
            border-radius: 45px;
            border: 10px solid #333;
            position: relative;
            overflow: hidden;
            box-shadow: 0 25px 50px rgba(0,0,0,0.5);
        } [cite: 7, 8]

        .header {
            background: linear-gradient(135deg, #FF5E13, #FF8C00);
            padding: 40px 20px 25px;
            color: white;
            text-align: center;
            border-bottom-left-radius: 30px;
            border-bottom-right-radius: 30px;
        } [cite: 9, 10]

        .header h1 { font-weight: 900; font-size: 26px; text-shadow: 2px 2px 4px rgba(0,0,0,0.2); } [cite: 11]

        .content-area {
            height: 520px;
            overflow-y: auto;
            padding: 20px;
            scrollbar-width: none;
        } [cite: 13, 14]

        .item-card {
            background: white;
            border-radius: 20px;
            margin-bottom: 20px;
            padding: 15px;
            display: flex;
            align-items: center;
            box-shadow: 0 8px 20px rgba(0,0,0,0.06);
            transition: 0.3s;
            animation: slideUp 0.5s ease forwards;
        } [cite: 16, 17]

        .item-img { width: 80px; height: 80px; border-radius: 15px; object-fit: cover; margin-right: 15px; } [cite: 19]

        .btn-size {
            background: #f0f0f0;
            border: 1px solid #ddd;
            padding: 6px 10px;
            border-radius: 8px;
            font-size: 10px;
            font-weight: 700;
            cursor: pointer;
            transition: 0.2s;
        } [cite: 23, 24]

        .cart-bar {
            position: absolute;
            bottom: 80px;
            left: 20px;
            right: 20px;
            background: var(--secondary);
            color: white;
            padding: 15px 20px;
            border-radius: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transform: translateY(150%);
            transition: 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        } [cite: 26, 27, 28]

        .cart-bar.active { transform: translateY(0); } [cite: 29]

        .checkout-btn {
            background: var(--primary);
            color: white;
            padding: 8px 15px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 700;
            font-size: 12px;
        } [cite: 30, 31]

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        } [cite: 36, 37]
    </style>
</head>
<body>

<div class="phone-frame">
    <div class="header">
        <h1>Y 16 FRIES</h1>
        <p>Garma Garam & Crispy Fries!</p>
    </div>

    <div class="content-area">
        <h2 style="font-size: 18px; margin-bottom: 15px;">Our Menu</h2>
        <div id="menu-items"></div>
        <div style="margin-top: 20px; padding: 15px; background: #fff8f5; border-radius: 15px; border: 1px dashed var(--primary);">
            <p style="font-size: 12px; font-weight: 700; color: var(--primary);">🎁 Limited Offer:</p> [cite: 38]
            <p style="font-size: 11px;">3 Signature Sauces FREE with every order!</p>
        </div>
    </div>

    <div class="cart-bar" id="cart-bar">
        <div><span id="items-count">0</span> Items | <span id="total-price">Rs 0</span></div> [cite: 39]
        <a href="#" class="checkout-btn" onclick="sendWhatsApp()">ORDER NOW</a>
    </div>
</div>

<script>
    const menu = [
        { id: 1, name: 'Classic Fries', half: 100, full: 180, img: 'classic.jpg' }, [cite: 40]
        { id: 2, name: 'BBQ Fries', half: 150, full: 250, img: 'bbq .jpg' }, [cite: 41]
        { id: 3, name: 'Peri Peri Fries', half: 100, full: 180, img: '0001532_peri-peri-french-fries_550.webp' },
        { id: 4, name: 'Mix Masala Fries', half: 150, full: 250, img: 'mix masala.jpg' }
    ];

    let cart = [];

    function renderMenu() {
        const container = document.getElementById('menu-items');
        menu.forEach((item, index) => {
            const card = document.createElement('div');
            card.className = 'item-card';
            card.style.animationDelay = (index * 0.1) + 's';
            card.innerHTML = `
                <img src="${item.img}" class="item-img" onerror="this.src='https://via.placeholder.com/80?text=Fries'">
                <div style="flex-grow: 1;">
                    <h3 style="font-size: 15px;">${item.name}</h3>
                    <div style="display: flex; gap: 8px; margin-top: 10px;">
                        <button class="btn-size" onclick="addToCart('${item.name}', ${item.half}, 'Half', this)">Half: ${item.half}</button> [cite: 44]
                        <button class="btn-size" onclick="addToCart('${item.name}', ${item.full}, 'Full', this)">Full: ${item.full}</button> [cite: 45]
                    </div>
                </div>
            `;
            container.appendChild(card);
        });
    }

    function addToCart(name, price, size, btn) {
        cart.push({ name, price, size }); [cite: 46]
        btn.style.background = '#4CAF50';
        btn.innerText = 'Added!'; [cite: 47]
        setTimeout(() => {
            btn.style.background = '#f0f0f0';
            btn.innerText = size + ': ' + price;
        }, 800); [cite: 48]
        updateCartUI();
    }

    function updateCartUI() {
        const cartBar = document.getElementById('cart-bar');
        const countSpan = document.getElementById('items-count');
        const priceSpan = document.getElementById('total-price');
        if (cart.length > 0) {
            cartBar.classList.add('active'); [cite: 50]
            countSpan.innerText = cart.length; [cite: 51]
            const total = cart.reduce((sum, item) => sum + item.price, 0);
            priceSpan.innerText = 'Rs ' + total; [cite: 52]
        }
    }

    function sendWhatsApp() {
        const number = "923214760034"; [cite: 53]
        let message = "🍟 *New Order from Y 16 FRIES* %0A%0A"; [cite: 54]
        let total = 0;
        cart.forEach((item, i) => {
            message += `${i+1}. ${item.name} (${item.size}) - Rs ${item.price}%0A`;
            total += item.price;
        }); [cite: 55]
        message += `%0A------------------------%0A*Total Bill: Rs ${total}*%0A%0A_Please confirm my order with 3 signature sauces!_`;
        window.open(`https://wa.me/${number}?text=${message}`, '_blank');
    }

    renderMenu();
</script>
</body>
</html>